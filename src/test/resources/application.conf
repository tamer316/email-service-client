play {
  server.provider = "play.core.server.NettyServerProvider"
  crypto.secret = "test"
}

mail {
  source.address = "test@email.com"
  source.personal = "Test"
  service.url = "http://localhost:8090"
}